export default async function POST(req: Request) {
  console.log(req.body, " --- REQUEST BODY IN SHIPPING ROUTE");
  // handle input
  // call external API (google maps address validation api)
  // handle output
}
