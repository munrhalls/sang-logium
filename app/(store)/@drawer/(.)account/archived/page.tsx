import { redirect } from "next/navigation";

export default function AccountPage() {
  // redirect("/account/orders");
  return (
    <div className="fixed inset-10 h-80 w-full bg-black bg-white p-8 text-white">
      ACCOUNT MODAL PAGE
    </div>
  );
}
