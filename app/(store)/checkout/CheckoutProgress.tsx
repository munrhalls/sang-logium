// client
// uses url to highlight current step
// shows all steps
// links to previous steps
// no links to future steps
// steps: Shipping, Payment, Summary
// if current step is Shipping, only Shipping is highlighted
// if current step is Payment, Shipping and Payment are highlighted
// if current step is Summary, all steps are highlighted
// different active highlight for current step
// different highlight for available steps (previous steps)
// future steps are greyed out unless they are completed

export default function CheckoutProgress() {
  return <div>CheckoutProgress</div>;
}
