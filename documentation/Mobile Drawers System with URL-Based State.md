User Click → URL Update (INSTANT)
↓
useSearchParams() re-render (INSTANT)
↓
Drawer Shell mounts (INSTANT)
↓
Animation starts (INSTANT)
↓
Server Component streams via Suspense
↓
Categories render when ready (with skeleton fallback)
